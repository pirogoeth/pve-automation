# This manifest was generated by flux. DO NOT EDIT.
#
# NOTE TO FUTURE SELF: This has been edited to add the decryption settings
# on the Kustomization resource. Otherwise Flux will not be able to reconcile anything.
---
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 1m0s
  ref:
    branch: main
  secretRef:
    name: flux-system
  url: ssh://git@github.com/pirogoeth/pve-automation
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: flux-system
  namespace: flux-system
spec:
  decryption:
    provider: sops
    secretRef:
      name: sops-age
  interval: 10m0s
  path: ./fluxcd/flux-system
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
